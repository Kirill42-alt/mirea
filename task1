/*

Task 1. Option 6. x=8z/(e^t+2)-y^2 where t=1; z=t+2; y=4

*/

#include <stdio.h>
#include <math.h>

int main() {
    
    int t = 1;
    int z = t+2;
    int y = 4;
    int e = 2.71;
    
    int x = 8*z/((pow(e, t))+2)-pow(y, 2);
    
    printf("x: %d\n", x);
}
